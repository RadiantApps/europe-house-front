import "./globals.css";

export const metadata = {
  title: "Europa House",
  description: "Europa House",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body
       
      >
        {children}
      </body>
    </html>
  );
}
